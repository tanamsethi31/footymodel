# Copy to .env and fill in values (never commit .env).
# Sync to GitHub Actions: gh secret set -f .env  (repo admin required)

API_FOOTBALL_KEY=   # https://www.api-football.com/pricing (free: 100 req/day)
RAPIDAPI_KEY=       # RapidAPI "Free API Live Football Data" listing
APIFY_TOKEN=        # https://console.apify.com/account/integrations
NOTIFY_SECRET=      # shared with Vercel NOTIFY_SECRET for push on new commits
